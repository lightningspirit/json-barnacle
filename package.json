{
  "name": "heroku-config-json",
  "version": "1.0.0",
  "description": "Converts cfg.json to Env vars format",
  "main": "index.js",
  "scripts": {
    "convert": "node convert.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.19"
  },
  "devDependencies": {
    "eslint": "^4.19.1"
  }
}
